/* 4.4. Strona Kategorii */
.category-header {
    text-align: center;
    margin-bottom: var(--spacing-lg);
    max-width: 100%;
    margin-left: auto; margin-right: auto;
    padding: var(--spacing-md) var(--spacing-sm) 0;
}
.category-header .category-title { font-family: var(--font-family-headings); font-size: 3rem; margin-bottom: var(--spacing-xs); }
.category-header .category-description { font-weight: var(--font-weight-light); font-size: 1.3rem; margin: 0; }
